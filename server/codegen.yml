overwrite: true
schema: "http://localhost:9000/graphql"
generates:
  src/graphql-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      voidOptionals: true
      typesPrefix: I
